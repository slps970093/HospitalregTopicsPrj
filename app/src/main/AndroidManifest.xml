<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yu_hsienchou.hospitalregtopicsprj">
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".doctorlstActivity" />
        <activity android:name=".FamilyActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".resDeleteActivity" />
        <activity android:name=".ReservationActivity" />
        <activity android:name=".reservationMenuActivity" />
        <activity android:name=".SetAlarmActivity"></activity>
        <!-- 當鬧鈴時間到達時要執行的程式 -->
        <receiver android:name=".MyRegBroadcast">
            <intent-filter>
                <action android:name="play_hskey" />
            </intent-filter>
        </receiver>
    </application>

</manifest>